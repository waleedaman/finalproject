  class UpdateModeAction{
    String updatedMode;
    UpdateModeAction(this.updatedMode);
  }

  class UpdateAuthStatusAction{
    bool authMode;
    UpdateAuthStatusAction(this.authMode);
  }

  class UpdateUserNameAction{
    String updatedUserName;
    UpdateUserNameAction(this.updatedUserName);
  }

  class UpdateUserIdAction{
    int updatedUserId;
    UpdateUserIdAction(this.updatedUserId);
  }

  class UpdateOpenProjectIdAction{
    int updatedOpenProjectId;
    UpdateOpenProjectIdAction(this.updatedOpenProjectId);
  }
